# HISTORICAL_REFERENCE_FILES — Pluviometrics Hub

**Audit branch:** `audit/dead-file-audit-hub`.

## Zero historical-reference files.

The hub has no `reports/`, no `docs/historical/`, no archived design notes. Its tree is a static landing page plus brand artwork.

The single `Superseeded/` directory is classified as `SAFE_DELETE` (see `SAFE_DELETE_CANDIDATES.md`) — it represents an old UI version, not a methodological record worth preserving.

**No action required.**
